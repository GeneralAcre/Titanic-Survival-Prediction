const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-RethinkSans)', 'sans-serif'],
      },
    },
  },
};

export default config;